import 'package:flutter/material.dart';

class PageBolsa extends StatelessWidget {
  const PageBolsa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("bolsa"),
    );
  }
}