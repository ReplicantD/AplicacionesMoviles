import 'package:flutter/material.dart';

class PageEmpresa extends StatelessWidget {
  const PageEmpresa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("empresas"),
    );
  }
}